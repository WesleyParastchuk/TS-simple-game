export type ArenaStyle = {
	backgroundColor: string;
	borderColor: string;
	borderStyle: string;
	borderWidth: string;
};

export type dimension = {
	num: number;
	unit: "px" | "rem" | "em" | "%";
};
